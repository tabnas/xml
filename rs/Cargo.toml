[package]
name = "tabnas-xml"
version = "0.7.9"
edition = "2021"
rust-version = "1.85"
license = "MIT"
description = "XML 1.0 grammar plugin for the tabnas parsing engine, with namespaces and entities"
repository = "https://github.com/tabnas/xml"

[lib]
name = "tabnas_xml"
path = "src/lib.rs"

[dependencies]
# The engine and the relaxed-JSON base grammar are consumed as SIBLING
# CHECKOUTS, the standard tabnas development model: clone
# https://github.com/tabnas/parser and https://github.com/tabnas/jsonic
# next to this repo. Neither crate is published, so there is no registry
# version to fall back on. jsonic brings tabnas-json with it.
tabnas = { path = "../../parser/rs" }
tabnas-jsonic = { path = "../../jsonic/rs" }
indexmap = "2"
regex = "1.10"
serde_json = { version = "1.0", features = ["preserve_order"] }

[dev-dependencies]
# The shared fixture loader and runner: one loader in three languages, so
# the rows mean the same thing in every runtime.
tabnas-support = { path = "../../support/rs", features = ["serde_json"] }
